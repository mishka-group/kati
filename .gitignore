# ── Wayfinder ───────────────────────────────────────────────────────────
# .scratch/research/ IS tracked — it is the evidence base every ticket cites.
# .scratch/tickets/  is NOT: those bodies were published as GitHub issues,
# which are canonical. A second copy here would drift.
.scratch/*
!.scratch/research/
!.scratch/issue-map.json

# ── Elixir ──────────────────────────────────────────────────────────────
/_build/
/cover/
/deps/
/doc/
/.fetch
erl_crash.dump
*.ez
*.beam
/config/*.secret.exs
.elixir_ls/

# Regenerated from castore on every compile by the `kati.certs` alias.
# Tracking it would pin Kati to whatever Mozilla trusted the day it landed.
/priv/cacerts.pem

# ── Mob ─────────────────────────────────────────────────────────────────
# Machine-local absolute paths, written by `mix mob.install`.
mob.exs
android/local.properties

# ── Android build output ────────────────────────────────────────────────
android/.gradle/
android/build/
android/app/build/
android/app/.cxx/
android/app/src/main/jni/.zig-cache/
android/app/src/main/jniLibs/
zig-out/
*.apk
*.aab
*.keystore

# ── iOS build output ────────────────────────────────────────────────────
ios/build/
ios/DerivedData/
*.xcuserstate

# ── macOS ───────────────────────────────────────────────────────────────
.DS_Store

# Left behind by `mix mob.release --android`, which stages the OTP tree here
# for gradle to bundle. Every later DEBUG apk then bakes in that 43MB
# snapshot, and MobBridge.extractOtpIfNeeded unpacks it over <filesDir>/otp
# on the next clean install — silently replacing freshly deployed BEAMs with
# whatever was compiled at release time. bin/deploy_native.sh deletes it.
android/app/src/main/assets/otp.zip
